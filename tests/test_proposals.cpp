    // cout << "Creating a proposal engine..." << endl;
    // ProposalEngine proposal_engine(params);
    // // cout << "Create and update particles..." << endl;
    // // for (int i = 0; i < 10; ++i) {
    // //     Particle p = proposal_engine.create_particle();
    // //     Particle up = proposal_engine.propose_particle(p);
    // //     Particle up2 = proposal_engine.propose_particle(p);
    // //     cout << "Random: " << p.ws << endl; 
    // //     cout << "up. v1: " << up.ws << endl;
    // //     cout << "up. v2: " << up2.ws << endl;
    // // }